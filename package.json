{
  "name": "dev-workflow-with-agents",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces",
    "dev-short": "npm run short --workspace=packages/slides"
  }
}
